# Copy to .env and fill in real values. .env is gitignored.

# OpenAI API key — used by the shadow validator (gpt-4o-mini by default)
OPENAI_API_KEY=

# GitHub Personal Access Token with `repo` scope — used to open PRs on the
# repo named in [mcp] target_repo in prmcp.toml.
PAT_TOKEN=
