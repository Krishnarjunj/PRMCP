# Legacy docs (frozen)

These files are the original hackathon-era notes that drove the first version of `prmcp` (Plivo Hackathon 2026, "Plivo By Agents" track). They are kept here for historical context only.

The current package is **generic** — it works against any SDK + MCP repo pair via `prmcp.toml`. See the top-level `README.md` for current install and usage.

Do not edit these files. Do not derive current behavior from them.
